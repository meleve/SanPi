<%= form_for(@producto) do |f| %>
  <% if @producto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@producto.errors.count, "error") %> prohibited this producto from being saved:</h2>

      <ul>
      <% @producto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class='panel panel-default'>
    <div class="panel-body">
      <div class="form-group col-lg-3">
        <%= f.label :Producto %>
        <%= f.text_field :nombreproduct, placeholder: 'Producto', class: 'form-control input-sm' %>
      </div>
      <div class="form-group col-lg-3">
        <%= f.label :Descripción %>
        <%= f.text_field :descripcion, placeholder: 'Descripcion', class: 'form-control input-sm' %>
      </div>
      <div class="form-group col-lg-3">
        <%= f.label :Cantidad %>
        <%= f.number_field :cantidad, placeholder: 'Cantidad', class: 'form-control input-sm' %>
      </div>
      <div class="form-group col-lg-3">
        <%= f.label :Precio %>
        <%= f.number_field :precio, placeholder: 'Precio', class: 'form-control input-sm' %>
      </div>
    </div>

    <div class="panel-footer"> 
      <div class="actions">
        <%= button_tag(type: 'submit', class: "btn btn-success btn-sm") do %>
          <span class="fa fa-check"></span> Guardar
        <% end %> 
      </div>
    </div>  
  </div>
<% end %>
